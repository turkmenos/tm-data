# Turkmen stop words

`stopwords.json` contains a small JSON object with `language: "tk"` and a
`words` array of five UTF-8 strings. Preserve Turkmen characters when loading
or comparing the words. The list is a starting point, not a comprehensive
or evaluated stop-word vocabulary.

## Sources and provenance

The existing file does not record an author, external source, retrieval date,
or extraction method. Those details are unknown. No external provenance or
separate redistribution permission has been verified. The catalog indexes
the existing file without changing or adding words.

Changes should include a source or rationale for each addition and should
avoid duplicate words. Regenerate the root catalog after editing this file.
